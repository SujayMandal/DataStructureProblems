use umg_admin;

SET SQL_SAFE_UPDATES = 0;

update POOL SET WAIT_TIMEOUT = 60000 where WAIT_TIMEOUT IS null or WAIT_TIMEOUT IS NULL or WAIT_TIMEOUT <> 90000;

update POOL set MODELET_COUNT = 5 where ID = 101;

update POOL set MODELET_COUNT = 3 where ID = 100;

update POOL_CRITERIA_DEF_MAPPING SET POOL_CRITERIA_VALUE = '#TENANT# = Any & #ENVIRONMENT# = R & #ENVIRONMENT_VERSION# = 3.2.1 & #TRANSACTION_TYPE# = ANY & #MODEL# = ANY & #MODEL_VERSION# = ANY & #TRANSACTION_MODE# = test' where id = 4;

update POOL_CRITERIA_DEF_MAPPING SET POOL_CRITERIA_VALUE = '#TENANT# = rentrange & #ENVIRONMENT# = R & #ENVIRONMENT_VERSION# = 3.2.1 & #TRANSACTION_TYPE# = BULK & #MODEL# = Any & #MODEL_VERSION# = Any & #TRANSACTION_MODE# = Any' where id = 101;

update POOL_CRITERIA_DEF_MAPPING SET POOL_CRITERIA_VALUE = '#TENANT# = realtrans & #ENVIRONMENT# = R & #ENVIRONMENT_VERSION# = 3.2.1 & #TRANSACTION_TYPE# = ANY & #MODEL# = eCMA_Ocn & #MODEL_VERSION# = ANY & #TRANSACTION_MODE# = ANY' where id = 15;

commit;