package com.ca.framework.core.rmodel.bo;

public interface RModelBO {
	
	/**
	 * Creates the modelInfo request string as required by modelet for load model with only 
	 * support package and model package name for R 
	 * @param modelName
	 * @param majorVersion
	 * @param minorVersion
	 * @return
	 */
	//public String getRModelInfo (String modelName, Integer majorVersion, Integer minorVersion, final String tenantCode);

}
